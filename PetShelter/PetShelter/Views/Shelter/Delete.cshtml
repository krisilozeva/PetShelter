@model PetShelter.Web.ViewModels.ShelterDetailsVM

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>
<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Products</h4>

    <div>
        <h4>Product</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.PetCapacity)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.PetCapacity)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Pets)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Pets.Count)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.LocationId)
            </dt>
            <dd>
                @Html.ActionLink("Location Details","Details","Location",new{id = Model.LocationId})
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">

                <input type="submit" value="Delete" class="btn btn-default" />|

                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "List")
            </div>
        }
    </div>
