@model PetShelter.Web.ViewModels.PetVaccineEditVM

@{
    ViewBag.Title = "Edit";
}


<h2>Edit</h2>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToke()

    <div class="form-horizontal">
        <h4>Product</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddeFor(model => model.Id)

        <div class="form-group">
            @Html.LabelFor(model => model.PetId, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.PetId)
                @Html.ValidationMessageFor(model => model.PetId)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}
<div>
    @Html.ActionLink("Back to List", "List")
</div>